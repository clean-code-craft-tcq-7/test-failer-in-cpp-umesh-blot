#ifndef MAIN_HEADER_H
#define MAIN_HEADER_H

// Returns the T-shirt size for a given measurement
char size(int cms);

// Prints the color map and returns the number of pairs
int printColorMap();


#endif // MAIN_HEADER_H
